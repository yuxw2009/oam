import {  detail, query } from '../services/signaltrace'
import { parse } from 'qs'
export default {
  namespace: 'signaltrace',
  state: {
//	status: '',
//  loading: false,
//  correlationId:'',
//  isMotion: localStorage.getItem('antdAdminUserIsMotion') === 'true',
  },
  subscriptions: {
  	
  /*	
    setup ({ dispatch, history }) {
      history.listen(location => {
        if (location.pathname === '/signaltrace') {
        	
        }
      })
    }, */
    
  },

  effects: {
   
  },
  reducers: {
  
 
  },
}
